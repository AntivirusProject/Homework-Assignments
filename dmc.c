#include <stdio.h>
//#include <stdlib.h>

int main(){
		//char * i = (char*)malloc(5 * sizeof(char));
	
		//scanf("%s", i);
		//printf("%s", i);
	
	
	int j;
	
	int b[2];
	b[7]=10;	
	for(j = 0; j < 7; j++){	
		scanf("%d", &b[j]);

	
		}
	for(j = 0; j < 8; j++){	
		printf("%d", b[j]);

	
		}
return 0;
}
